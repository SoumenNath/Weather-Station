# Weather-Station
This is an implamentation of a simple weather database. The database stores several weather stations and allows for basic queries about the data. Each weather station keeps some records of the weather (at the location of the station). A weather record consists of the temperature at a given time.
